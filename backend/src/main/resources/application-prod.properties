spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/campus_booking}
spring.datasource.username=${DATABASE_USERNAME:campus}
spring.datasource.password=${DATABASE_PASSWORD:campus}
spring.h2.console.enabled=false
spring.jpa.hibernate.ddl-auto=validate
